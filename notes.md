# Notes: Research Lane

## Scope

Research taxonomy and completion gates are defined in `GOAL.md`. This file records working findings, provider diagnostics, and decisions during execution.

## Findings

- Repository begins with six YouTube transcripts and no source schema, recursive-loop runner, company index, benchmark, or validator.
- Exa MCP and authenticated GitHub CLI are usable.
- `agent-reach` launcher is absent; installed direct backends include Twitter/OpenCLI/yt-dlp.
- Twitter is blocked by absent cookies. No Perplexity CLI, MCP server, or environment credential was found.
- Codex CLI supports native `--search` and is authenticated; use it for the requested OpenAI/GPT web-research attempt.
