# Canonical source tree

Every supported platform has `accepted/` and `rejected/` states. Source documents combine flat YAML front matter conforming to `schema/source.schema.json` with preserved evidence in the Markdown body. Rejected documents are preserved evidence, but generated validated totals exclude them.
